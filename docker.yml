services:
  backend:
    image: rtgtx7/mulanstudio:backend-latest
    container_name: mulandance-backend
    environment:
      ENVIRONMENT: production
      DEBUG: "false"
      SECRET_KEY: ${SECRET_KEY:-change_me_to_a_long_random_secret}
      ADMIN_EMAIL: "admin@mulanstudio.ca"
      ADMIN_PASSWORD: "MuLanStudio!2026#Admin_7Qx9"
      ADMIN_FIRST_NAME: ${ADMIN_FIRST_NAME:-Mulan}
      ADMIN_LAST_NAME: ${ADMIN_LAST_NAME:-Admin}
      DATABASE_URL: sqlite:////app/data/app.db
      DATA_DIR: /app/data
      NEWS_FILES_DIR: /app/data/news
      UPLOADS_DIR: /app/data/uploads
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://localhost:3000}
      ALLOWED_HOSTS: ${ALLOWED_HOSTS:-http://localhost:3000,http://127.0.0.1:3000}
      USE_FILE_STORAGE: "true"
      AI_MAX_URLS: ${AI_MAX_URLS:-10}
      AI_MAX_IMAGES_PER_URL: ${AI_MAX_IMAGES_PER_URL:-5}
      AI_ENABLED: ${AI_ENABLED:-false}
      AI_PROVIDER: ${AI_PROVIDER:-openai_compatible}
      AI_API_BASE_URL: ${AI_API_BASE_URL:-https://api.openai.com/v1}
      AI_API_KEY: ${AI_API_KEY:-}
      AI_MODEL: ${AI_MODEL:-}
      AI_TIMEOUT_SECONDS: ${AI_TIMEOUT_SECONDS:-60}
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-587}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM_EMAIL: ${SMTP_FROM_EMAIL:-}
      SMTP_USE_TLS: ${SMTP_USE_TLS:-true}
    volumes:
      - /volume1/docker/mulandance/data:/app/data
    expose:
      - "8000"
    restart: unless-stopped

  frontend:
    image: rtgtx7/mulanstudio:frontend-latest
    container_name: mulandance-frontend
    environment:
      NODE_ENV: production
      API_PROXY_TARGET: ${API_PROXY_TARGET:-http://backend:8000}
      NEXT_PUBLIC_APP_URL: ${NEXT_PUBLIC_APP_URL:-http://localhost:3000}
    depends_on:
      - backend
    ports:
      - "${FRONTEND_PORT:-3000}:3000"
    restart: unless-stopped
